// 평균탐색시간(평균비교횟수)이 최소인 Binary Search Tree 찾는 문제
// 각 트리의 평균 탐색(검색) 시간 : c[i]*p[i]의 총합! (i는 각 검색key, 즉 노드들의 인덱스.)
// ***여기서, 저 총합을 n으로 나눌 필요 없음!! 이미 p라는 확률을 곱함으로써, 평균임! 기댓값!(p의 분모에 이미 n이 있으므로..^^그만생각ㅋ)

// A 배열(표) 채워나가는 순서 : 대각선부터해서 우측하단으로, 차이가 0인거부터 점점 차이 큰걸로!!(연쇄행렬곱셈도 이런식)
